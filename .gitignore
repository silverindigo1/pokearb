__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.DS_Store

# The built site is produced by the workflow and published to Pages.
# data/ is deliberately NOT ignored: the price history lives in the repo.
site/
